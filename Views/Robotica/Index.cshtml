<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Club de Robótica - USMP</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        body {
            font-family: 'Poppins', sans-serif;
            background: #5a0000;
            color: white;
            text-align: center;
        }
        header {
            background: #1a1a2e;
            color: white;
            padding: 20px;
            text-align: center;
            box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.3);
        }
        nav {
            display: flex;
            justify-content: center;
            gap: 20px;
            padding: 15px 0;
            background: #ffffff;
            position: sticky;
            top: 0;
            z-index: 1000;
        }
        nav a {
            color: white;
            text-decoration: none;
            font-weight: 600;
            padding: 10px 15px;
            border-radius: 5px;
            transition: 0.3s;
        }
        nav a:hover {
            background: #e94560;
        }
        section {
            padding: 50px 20px;
            max-width: 900px;
            margin: auto;
            background: white;
            color: #333;
            box-shadow: 0px 4px 15px rgba(0,0,0,0.2);
            border-radius: 12px;
            margin-top: 30px;
            opacity: 0;
            transform: translateY(30px);
            transition: all 0.6s ease-in-out;
        }
        section.visible {
            opacity: 1;
            transform: translateY(0);
        }
        .btn {
            display: inline-block;
            padding: 12px 25px;
            margin-top: 15px;
            background: #e94560;
            color: white;
            text-decoration: none;
            border-radius: 8px;
            transition: 0.3s;
        }
        .btn:hover {
            background: #d63050;
            transform: scale(1.05);
        }
        footer {
            margin-top: 40px;
            background: #ffffff;
            color: white;
            padding: 15px;
            text-align: center;
        }
    </style>
</head>
<body>
    <header style="margin-top: 2em;">
        <h1>Club de Robótica - USMP</h1>
    </header>
    <nav>
        <a href="#proyectos">Proyectos</a>
        <a href="#aula">El Aula</a>
        <a href="#horarios">Horarios</a>
        <a href="#talleres">Talleres</a>
        <a href="#contacto">Contacto</a>
    </nav>
    <section id="proyectos" class="content">
        <h2>Proyectos Realizados</h2>
        <p>Explora nuestros proyectos más innovadores en robótica.</p>
    </section>
    <section id="aula" class="content">
        <h2>Sobre Nuestro Aula</h2>
        <p>Contamos con tecnología avanzada para desarrollar proyectos en IA y robótica.</p>
    </section>
    <section id="horarios" class="content">
        <h2>Horarios Disponibles</h2>
        <p>Consulta los horarios y reserva tu espacio.</p>
    </section>
    <section id="talleres" class="content">
        <h2>Talleres Disponibles</h2>
        <p>Únete a nuestros talleres de robótica e innovación.</p>
        <a href="#" class="btn">Inscribirse</a>
    </section>
    <footer>
        <p>© 2024 Club de Robótica - USMP</p>
    </footer>
    <script>
        document.addEventListener("DOMContentLoaded", function() {
            let sections = document.querySelectorAll(".content");
            let observer = new IntersectionObserver(entries => {
                entries.forEach(entry => {
                    if (entry.isIntersecting) {
                        entry.target.classList.add("visible");
                    }
                });
            }, { threshold: 0.3 });
            sections.forEach(section => {
                observer.observe(section);
            });
        });
    </script>
</body>
</html>
